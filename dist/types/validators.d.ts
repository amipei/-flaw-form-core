export default class Validators {
    static compose(a: any[]): void;
    static singleCompose(a: any[]): void;
    static composeAsync(a: any[]): void;
    static singleComposeAsync(a: any[]): void;
}
